/**
 * Daftar referensi statis - BUKAN modul CRUD penuh (itu di luar scope
 * User Management). Position != Role: ini jabatan struktural, bukan
 * hak akses sistem (lihat types/role.ts untuk Role).
 */
export const positionOptions = ["Staff", "Supervisor", "Manager", "Director"];
